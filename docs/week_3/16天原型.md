# 16 天原型:进度与问题

> 开工 2026-08-10　|　截止 2026-08-26　|　脚本:[`p0_shadow.py`](../../p0_shadow.py)

## 要回答的唯一问题

> **在 OPT 预言机更新后、ARB 预言机更新前的那 ~34 秒里,
> 我能不能算出哪些仓位即将可清算?**

不是"做一个能赚钱的机器人",是**先证明"能提前看见"**。

前置证据(见 [清算机会时间分布教学](../week_2/清算机会时间分布教学.md)):

```
✅ 跨链预言机时差   平静日 63%/31s,爆发日 78%/34s
✅ spam 经济性      失败一次 $0.0084,年空转成本 ~$850
✅ 时间集中度       最忙 5 天占 53.6% 的机会
✅ 爆发日参与者     只有 56 个(全期 207 个),多数平时活跃的反而消失
```

> ## 🔴 2026-08-10 更新:上面这个问题,前提是错的
>
> **Arbitrum 上的 Aave 根本不读那个公开喂价。** 它从 2026-03 起改读
> Chainlink SVR 喂价,而 SVR 的整个设计目的就是把清算 OEV 拍卖回收。
> 官方口径:**Arbitrum 回收率 89.2%,Base 89.3%**。
>
> 我测了 180 天的"跨链时差",测的是**两个 Aave 都不用的喂价**之间的时差。
>
> 详见下面 [「SVR:被推翻的前提」](#svr被推翻的前提)。
> **OPT 那条腿是对的**(Optimism 未上 SVR,Aave 读的确实是公开喂价),
> ARB 那条腿作废。

---

## 三阶段

| 阶段 | 做什么 | 发交易? | 工作量 | 状态 |
|---|---|---|---|---|
| **P0 影子模式** | 监听 OPT 预言机 → 用新价格重算 ARB 仓位健康度 → 记录"谁将要可清算" | ❌ 只记录 | 3~5 天 | **✅ 跑通** |
| **P1 模拟验证** | 对每个目标用 `eth_call` 模拟清算,验证真会成功 | ❌ 只模拟 | 3~5 天 | ⬜ |
| **P2 小额实盘** | 部署合约,闪电贷 + 清算 + RFQ 变现打包一笔 | ✅ 亏得起的量 | 需要合约 | ⬜ 可能超期 |

**P0+P1 跑完能回答成本模型里最后一个「待定」项 —— 失败交易概率。** 全程不花钱。

---

## P0 进度(2026-08-10)

### 已完成

**① 高危仓位名单** —— 链上直读,不依赖第三方索引

```
扫 Arbitrum 3 天的 Aave V3 Borrow 事件  →  484 个借款人(104 次请求,25 秒)
逐个 getUserAccountData 拿真实健康度    →  459 个有活跃债务
筛 HF ≤ 3.0                            →  420 个进名单
```

当前分布(ETH = $1,918):

```
HF < 1.05:   7 个   债务 $   107,101
HF < 1.10:  20 个   债务 $   184,362
HF < 1.20:  65 个   债务 $ 1,311,163
HF < 1.50: 202 个   债务 $ 4,958,113
最危险: HF 1.0244,债务 $40,145
```

**② `--what-if` 单次评估**

```
跌  1%  →   0 个仓位  $          0
跌  3%  →   1 个仓位  $     40,145
跌  5%  →   7 个仓位  $    107,101
跌  8%  →  17 个仓位  $    166,654
跌 12%  →  39 个仓位  $    533,127
跌 20%  →  86 个仓位  $  2,289,340
```

**这张表直接回答"这门生意的规模"** —— 一次 5% 的下跌只解锁 $10 万可清算债务;
要到 $200 万级别得跌 20%。和 [180 天扫描](../week_2/清算机会时间分布教学.md)
里"最忙一天 1,114 笔"对得上:**那种日子就是跌了 15~20% 的日子。**

**③ 影子监控** —— 持续记录,已跑通

```
[04:20:09] OPT $1,918.42  ARB $1,917.95  差 +2.5bps  可清算 OPT=0 ARB=0
```

每轮落一条 JSONL 到 `shadow/predictions.jsonl`,记录:
两条链的价格、价差、各自口径下的可清算数量、以及**「OPT 已看见但 ARB 还没反映」的那批**
—— 最后那个字段就是那 34 秒窗口的实证。

异常隔离生效:12 轮里有 2 次 `Connection reset`,循环没有中断
(复用 [`lib/chainkit.py`](../../lib/chainkit.py) 的 `run_loop`)。

---

## 踩到的坑

### 坑 1:现成的 Aave 风险 subgraph 数据是坏的

The Graph 上有个 `aave-liquidation-risk-arbitrum`,名字完全对口,schema 也漂亮
(`position` / `healthFactor` / `riskLevel`)。差点就直接用了。

交叉验证之后:

```
subgraph 说                        链上 getUserAccountData 现算
15 个仓位 healthFactor 全是 1.0000  8 个抽查里 5 个**根本没有债务**(HF=∞)
debt = 6.72e18                     ← 原始 wei,没归一化
                                   另两个差 1.4% / 23.4%
```

**`healthFactor` 是写死的常数,不是真实计算。**

> **可迁移的一条:第三方索引数据必须交叉验证,而且要用"应该对不上就是错"的判据。**
> 这次的判据很硬:**subgraph 说有债务,链上说没有** —— 二者不可能同时对。
> 如果只看 subgraph 内部是否自洽,15 个整整齐齐的 1.0000 反而显得很"干净"。

改成从链上直读 Borrow 事件 + `getUserAccountData`,虽然慢(484 个地址查了 4 分钟),
但每个数字都可复核。

### 坑 2:扫描成本的估算错了一次

一开始算 Arbitrum "10,000 块 = 0.03 天,扫 180 天要 6,200 次请求",判断"太贵不做"。

**但 P0 只需要最近 3 天** —— 345,600 × 3 / 10,000 = **104 次请求,25 秒**。

> 「扫 180 天太贵」和「扫 3 天很便宜」都对,**错在没先问清楚"到底需要多长的窗口"**。
> 建名单只需要"最近谁在借钱",不需要历史。

---

## 已知的近似(P1 要消除)

**`getUserAccountData` 只给聚合值,不告诉你抵押物具体是什么币。**

所以推算新健康度用的是一阶近似:

```
HF_new ≈ HF_now × (P_new / P_now)
```

**只在「抵押物全是随该喂价波动的资产、债务是稳定币」时成立。**

真实仓位可能:
- 混着稳定币抵押物 → 价格跌时 HF 降得没那么快
- 借的也是波动资产 → 分子分母同向变动,HF 几乎不动

**所以 P0 的预测偏激进,会高估"将被清算"的数量。**

这个近似在 P0 阶段可以接受 —— **P0 的目的是证明"能提前看见",不是精确定价。**
P1 用 `eth_call` 逐个模拟 `liquidationCall` 才能消除它。

---

## 下一步

### P0 剩余

- [ ] **挂起来跑几天**,攒预测记录
- [ ] **回验命中率**:预测"将被清算"的仓位,后来真的被清算了吗?
      → 扫 `LiquidationCall` 事件和预测记录对齐
- [ ] **名单要定期刷新** —— 仓位会变(还款、追加抵押),建议每天 `--refresh` 一次

### P1 要做的

- [ ] 用 `eth_call` 模拟 `liquidationCall(collateral, debt, user, amount, false)`
      → 能拿到真实的抵押物种类和可清算金额,消除一阶近似
- [ ] 顺便拿到**每笔的实际清算奖励**,填进成本模型
- [ ] 统计模拟成功率 → **这就是「失败交易概率」的第一个估计**

### 需要留意的

- **420 个仓位每轮全量重算**,现在 20 秒一轮没问题,但如果名单涨到几千个,
  要改成只重算 HF 接近阈值的那批
- **Ankr 公共端点会 reset** —— 已有重试,但长跑要看断连频率
- **名单基于"最近 3 天借过钱的人"**,漏掉了老仓位。
  真要全覆盖得扫更长历史,或者从 aToken 持有者反推

---

---

# SVR:被推翻的前提

## 一句话

> **我盯错了喂价。** Aave 在 Arbitrum 上早就不读公开的 Chainlink ETH/USD 代理了,
> 它读的是一个专门用来**把清算利润拍卖回收给协议**的喂价。

## 什么是 SVR

SVR = **S**mart **V**alue **R**ecapture,Chainlink + Aave 的联合机制。
翻译成人话:

**以前**:预言机把新价格广播到公开内存池 → 所有 searcher 同时看见 → 谁快谁抢到清算。
利润归 searcher,协议一分钱拿不到。

**SVR 之后**:预言机把**同一份价格报告**同时走两条路发出去 ——

```
        ┌──→ 公开聚合器   (走公开内存池,谁都能看,和以前一样)
Data DON┤
        └──→ SVR 聚合器   (走私有通道,先不公开)
                              ↓
                         searcher 竞价:
                    「让我跟在这次价格更新后面清算,
                      我愿意把利润的 XX% 交出来」
                              ↓
                    出价最高的赢 → 他的清算和价格更新
                       打包进同一个区块
```

Aave 的合约读的是 **SVR 聚合器**。所以"价格变了导致仓位可清算"这件事,
在公开世界看见之前,拍卖赢家已经在同一个区块里把它做完了。

如果私有通道失败或超时,SVR 喂价会**回落到公开价格**(有个可配置的延迟)——
这就是漏给普通 searcher 的那部分。

## 官方公布的数字

来自 [Aave 治理提案 ARFC-SVR-Expansion](https://governance.aave.com/t/arfc-svr-expansion-next-phase-of-multi-network-expansion/25332):

| 市场 | 上线时间 | **回收率** |
|---|---|---|
| Ethereum Core V3 | 2025-03 | 51.5%(近 365 天) |
| **Base V3** | **2026-03** | **89.3%** |
| **Arbitrum V3** | **2026-03** | **89.2%** |

**为什么 L2 的回收率(89%)远高于以太坊主网(51.5%)?**
提案自己给了解释:以太坊有完整的 MEV 供应链(builder、validator 层层抽成),
价值在中途就被别人截走了;**Base / Arbitrum 是中心化排序器,中间环节少,
所以能收得更干净。**

> 这一条对我们特别刺眼:**恰恰是"排序器中心化、没有公开内存池"这个我原本
> 以为对小 searcher 有利的特征(不用拼 builder 关系),让 SVR 能吃得最干净。**

其他数字:
- 累计 SVR 清算量 ~$887.6M,总收入 ~$21.3M,Aave DAO 分 65% = ~$13.9M
- 参与 SVR 拍卖的独立地址:**从最初 93 个涨到 245 个**

## 我在链上怎么验证的

**别信文档,自己问协议。** 完整调用链:

```
Pool.ADDRESSES_PROVIDER()  →  PoolAddressesProvider
    .getPriceOracle()      →  AaveOracle
    .getSourceOfAsset(WETH)→  ★ Aave 真正读的喂价地址
```

结果:

| 链 | Aave 实际读 | 公开 Chainlink 代理 | |
|---|---|---|---|
| Arbitrum | `0xbd41b154…` | `0x639Fe6ab…` | 🔴 **不是同一个** |
| Base | `0x9da00d23…` | `0x71041ddd…` | 🔴 **不是同一个** |
| Optimism | `0x13e3Ee69…` | `0x13e3Ee69…` | ✅ 一致 |

已固化成命令,跑 `--watch` 前先跑它:

```bash
python3 p0_shadow.py --verify-feeds
```

### 两个喂价的行为差异(实测)

```
更新节奏(相邻两次的中位间隔)   SVR  90s   |   公开 120s
同一价格谁先发布(175 对配对)   SVR 领先 78%,中位 +4s
```

SVR **更新更勤、发布更早** —— 和"私有通道先拿到"完全吻合。

### 清算发生在什么时候

扫 Arbitrum Aave V3 最近 14 天全部 `LiquidationCall`,共 **38 笔**。
看每笔清算前 20 个区块内有没有 ETH/USD SVR 喂价更新:

```
14/38 笔(36.8%)命中,偏移分布:
  -10块 ×3   -9 ×2   -8 ×6   -4 ×1   -3 ×1   +2 ×1
```

**全部集中在喂价更新后的 3~10 个区块 = 0.75~2.5 秒内。**

这个 36.8% 有多大意义?算一下**基准率**:SVR 每 90 秒更新一次 = 每 360 个区块一次,
26 个区块的窗口撞上的概率约 7.2%。**实测 36.8% 是随机的 5 倍**,是真关联。

> **所以真实的反应窗口是 ~2 秒,不是我以为的 34 秒。差了 17 倍。**

---

## 又踩一个坑:基准率污染

我第一版做归因时想"ETH/USD 太窄,应该看**所有**喂价",于是改成
"清算前 12 块内有没有**任意**喂价更新"。结果:

```
同区块 13.2%,前 1~12 块 36.8%,无更新 50%
触发喂价:SHIB/USD、WIF/USD、ADA/USD、XAI/USD、M NAV、TRUAPT…
```

数字很好看,但**这些币根本不是 Aave Arbitrum 的储备资产**。

Arbitrum 上有几百个 Chainlink 喂价在不停更新,"最近的任意一个喂价更新"
几乎必然命中 —— **这个指标的基准率接近 100%,所以它不携带任何信息。**

> **可迁移的一条:放宽筛选条件让命中率变好看时,先算基准率。**
> 一个"随机也能达到"的命中率不是发现。
> 上一版只盯 ETH/USD 的 36.8%(基准 7.2%)才是有效测量。

---

# 「找 SVR 漏出来的市场」—— 结果是否定的

思路很自然:SVR 只在几条链上生效,那就去它没覆盖的市场。

**SVR 已生效**:Ethereum Core V3 / Prime V3 / V4、Base V3、Arbitrum V3
**提案中(尚未生效)**:Avalanche、Polygon、BNB Chain
**官方明确"不推荐上"**:**Sonic V3**、**Optimism V3**、Linea V3、Plasma V3
——理由是"revenue potential 有限"

于是我去给这些"漏网市场"称重,同样扫 14 天 Aave V3 的 `LiquidationCall`:

| 市场 | SVR 状态 | 14 天清算 | 每天 | 清算人数 | Top1 | Top3 |
|---|---|---|---|---|---|---|
| Arbitrum | ✅ 已生效 89.2% | 38 笔 | 2.7 | 11 | 34% | **66%** |
| Optimism | ❌ 官方不推荐 | 34 笔 | 2.4 | 8 | 35% | **79%** |
| Avalanche | ⏳ 提案中 | 19 笔 | 1.4 | **4** | 47% | **95%** |

**三个结论,都是坏消息:**

1. **漏网市场没有更多机会** —— Optimism 2.4 笔/天、Avalanche 1.4 笔/天,
   都不比被 SVR 吃掉 89% 的 Arbitrum(2.7 笔/天)多。

2. **漏网市场垄断更严重** —— Top3 占比 Arbitrum 66% → Optimism 79% → Avalanche 95%。
   **Avalanche 全链 14 天只有 4 个清算人,前两名吃掉 84%。**

3. **是同一批人** —— `0xdcd7b6b46f…` 同时是 Arbitrum 第一(34%)和 Optimism 第二(26%)。
   多链部署对他们是顺手的事。

> **因果搞反了。** 我以为"没被 SVR 覆盖 = 有肉漏出来";
> 实际是 **"没被 SVR 覆盖 = 小到 Aave 懒得回收"**。
> 官方给的理由白纸黑字:*limited revenue potential*。
> 而市场越小,越容易被一两个老玩家完全占住。

这也顺带说明:之前放弃 Sonic 是对的 —— 它同时出现在
"SVR 不推荐"和"Aave 提议关闭部署"两个名单上。

## 一个反直觉的出路:不打 SVR,加入 SVR

SVR 拍卖是**公开竞价**,不是邀请制。参与地址从 93 个涨到了 **245 个**。

对"小而中立的 searcher"来说,这个方向的性质完全不同:

```
抢公开清算          SVR 拍卖竞价
拼延迟,赢家通吃  →  拼出价,按利润分成
输了亏 gas        →  没中标不花钱
需要基础设施       →  需要的是估值准确
```

**拍卖把"谁跑得快"换成了"谁算得准"** —— 而算得准恰好是我们这四周一直在练的。
这条值得单独做一次可行性调查:**怎么成为 SVR 拍卖的参与方,门槛是什么。**

---

# P2 前置阅读:There Will Be Spam(MPI-SWS)

> [`论文/There Will Be Spam Characterizing State-Invariant.pdf`](../../论文/There%20Will%20Be%20Spam%20Characterizing%20State-Invariant.pdf)

## 它定义了什么

**state-invariant transaction(状态不变交易)**:把它从区块里删掉,
除了手续费之外**链上状态完全不变**的交易。所有 revert 的合约调用都算,
但也包括那些"成功了却什么也没改"的探测交易。

作者的判定方法不靠猜,用 `trace_replay_block_transactions` 拿每笔的 `stateDiff` 来算。

规模:**14 亿笔**。占以太坊交易的 2.6%,但占 **Optimism 的 24%、Base 的 37%**。
其中 Optimism 57%、Base 68% 来自投机型 MEV。

## 为什么这篇对 P2 是必读的

**因为它算了我一直没算的那笔账:空转成本要摊进利润里。**

我之前测出"失败一次 $0.0084,年空转 ~$850",但没做的是
**把这笔钱从收入里扣掉,再看还剩几个 bot 是赚的**。这篇论文做了(Table 6):

**扣除空转成本前 → 后,盈利 bot 的比例:**

| bot 类型 | Ethereum | Optimism | Base |
|---|---|---|---|
| 投机型**套利** | 84.1% → 82.1% | 56.5% → **13.1%** | 83.5% → **23.4%** |
| 投机型**清算** | 26.2% → 23.8% | 65.6% → **61.7%** | 74.5% → **71.1%** |

**这张表是我目前看到的、对"清算 vs 套利"最直接的一次对照实验。**

- **L2 上的投机型套利,扣掉空转成本后整体转负**(Optimism −31.26,Base −28.07),
  盈利 bot 从 56.5% 崩到 13.1%。
- **投机型清算几乎没掉**(65.6%→61.7%),而且**总利润仍为正**(OP +106.29,Base +390.00)。

原因论文也给了:**清算机会稀缺,所以不需要疯狂空转**;
而套利机会密集,投机型套利 bot 在 Optimism 有 82% 的交易是重复 calldata 的盲打。

> **这条独立支持了我们"选清算不选原子套利"的判断** ——
> 而且是从一个我们完全没测过的维度(空转成本占比)得出的。

另一个细节:以太坊上投机型清算只有 26.2% 盈利,L2 上却有 65~75%。
**投机式打法是 L2 特有的**,主网上因为有公开内存池 + 高 gas,盲打不划算。

## 对 P2 的三条直接影响

1. **失败交易概率不能只算"一次多少钱",要算"占收入多少"** ——
   成本模型里那一栏要改成比值。
2. **必须做 revert 保护**(论文里反复出现"因滑点保护 revert")——
   这正好是反编译那个 bot 时发现它**缺失**的东西。
3. **P2 的成功判据要写成:扣掉全部空转成本后仍为正**,而不是"抢到过几笔"。

---

## 时间账(已修订)

```
08-10  P0 跑通 + 🔴 发现 SVR,ARB 腿作废,喂价常量已修正
08-11  决定主战场:Optimism 实测 / 还是转 SVR 拍卖参与方
08-12 ~ 08-14  P0 重跑(改到确定没被 SVR 吃的市场)+ 回验命中率
08-15 ~ 08-19  P1 模拟验证 + 空转成本占比(按 spam 论文的口径)
08-20 ~ 08-26  P2 / 收口结业文档
```

**这次 SVR 的发现本身就是合格的交付物** ——
它是一个"验证了才知道不成立"的前提,而且是靠**问协议本身**而不是读文档发现的。

**即使 P2 做不完,P0+P1 的产出已经是合格的 Week 3 交付物** ——
一份「清算 edge 是否存在」的实证结论,以及成本模型最后一项的估计值。
