# 链上套利残酷共学

> 参与者:Starkxun　|　周期:2026-08-05 → 08-26(21 天)　|　入口:LI.FI + Hermes
> 这份文档是**给你自己推进用的**,不是给别人看的招募帖。每天照着做、照着打卡、照着复盘。

---

## 0. 怎么用这份文档

残酷共学的规则很硬:**每天围绕主题学习并提交学习证明到 GitHub 仓库,每周最多请假两次,断卡即淘汰。** 所以这份文档的设计原则是——每天都有一个能产出 markdown 笔记 / 代码 diff / 数据记录的**具体交付物**,直接就能当打卡证明 PR 提交。

三条铁律,贴在文档最上面天天看:

1. **只认到手,不认标价。** 同种资产转移用 token 数量算,永远别信 `xxxUSD` 字段。(这是你 Day-0 用真实数据劈出来的一刀,-35bps 的美元幻觉 vs +25bps 的真实成本,连正负号都反。)
2. **屏幕价差 ≠ 净收益。** 任何机会先问一句:扣完 gas / 桥费 / 滑点 / 失败交易 / 资金占用,还剩多少?
3. **可复现才是策略,复现不了只是噪声。** 一次赚钱不算 edge,能重复、能解释原因才算。

---

## 1. 核心命题(整个共学就为了回答它)

> 链上每天都在产生价差,但"看见价差"只是起点。真正的问题是:**机会从哪来,扣掉一切成本后是否仍然成立,以及如何让系统持续发现、验证、捕获它。**

你已经在 Day-0 触到了这个命题的骨头:一条 Arbitrum→Base 的 USDC 搬砖,真实门槛是 **25 bps 平费、无滑点、无规模窗口**。稳定币之间几乎不可能有 25bps 的真实可成交价差留给你,所以这条路基本不成立——**这个"不成立"本身就是有价值的结论。** 共学 21 天,你要做的就是把这套"证伪 / 证成"的能力,从一条路扩展到一整张地图。
> 这里 25 bps 平费的套利路径稍微解释一下：同一种稳定币（比如 USDC）在不同链上的价格出现可成交价差时，低买高卖. 例如：
Arbitrum 上的 USDC ≈ 1.0000
Base 上的 USDC ≈ 1.0025（贵了 25 bps）
理论上可以：在 Arbitrum 用 1 USDC 买入，跨链转到 Base，在 Base 卖掉，得到约 1.0025 USDC，中间扣除真实跨链成本后，如果还有利润，就是套利

---

## 2. 我的独特优势(这份文档为什么和别人不一样)

大多数参与者是从零开始学套利。你不是。你带着三样别人没有的东西进场,文档会反复调用它们:

- **Sonic 清算套利实盘**:你已经在做单链清算 MEV。清算就是套利地图上的一个分支,你不是学生,是已经在场上的人。
- **onChainListen(Python + web3.py 链上扫描工具集)**:别人要从头搭数据采集,你已经有一套能扫链、能持续轮询的基础设施。共学里"把一次性观察变成可重复查询/告警"这一步,你直接接现成的。
  > 2026-08-03 更正:原先记成"Go / PostgreSQL 管道"是错的 —— 实测 0 个 Go 文件、0 处 Postgres,实际是 ~30 个 Python 脚本 + web3.py,结果落 JSON/JSONL/TSV 文件。所以**能复用的不是数据库查询,是轮询采集的骨架**(checkpoint / 去重 / 循环容错),已抽成 [`lib/chainkit.py`](lib/chainkit.py)。
- **智能合约安全背景 + 桥安全研究(bridge-lab)**:LI.FI 是桥聚合器,你看桥的视角比套利者深一层——你能同时看到"路径的机会"和"路径的风险"。

**你的差异化主线**:不做拥挤的稳定币跨链搬砖(那是红海),而是往 **跨链清算 / 长尾链清算 + 经济漏洞型 MEV** 走,用共学的方法论把你的 Sonic 主业升级成"验证过 edge 的策略"。

---

## 3. 21 天路线图

> 时间是硬约束。每周一个大主题,每天一个小交付。周末留给复盘和补卡。

### 课前(08-01 → 08-04)· 地基 ✅ 大部分已完成

- [x] 跑通 LI.FI `/quote`,读懂 `estimate` 里的 gas / fee 结构
- [x] 建立"只认到手不认标价"的直觉(token 真相 vs 美元幻觉)
- [x] 算出一条真实路径的回本门槛(ARB→BAS USDC = 25 bps)
- [x] `curl https://li.quest/v1/chains`,确认 **Sonic 是否在 LI.FI 支持列表**。在→跨链清算这条线打通;不在→记下来,这是你后续要绕过的约束
- [ ] 把 Day-0 的探针脚本 + 三条铁律,整理成第一篇打卡笔记

### Week 1(08-05 → 08-11)· 建立套利地图 + 吃透 LI.FI

**目标**:画出属于你自己的链上套利机会地图,并把 LI.FI 从"能报价"用到"能系统查"。

每日任务(挑着做,一天一个):
- [x] 认全机会结构:DEX 内 / DEX 间 / 三角 / 稳定币 / **清算** / 跨链 / 一般 MEV。每类写清:价差从哪来、谁在跟你抢、天然成本是什么
- [x] 补底层:AMM 定价、订单簿、流动性深度、价格冲击、区块确认——重点搞懂"深度如何决定你的最优规模"
- LI.FI 系统化:`/chains`、`/tokens`、`/quote`、`/routes`、`/tools`、`/connections` 全部手动打一遍,搞清每个端点给你什么原始数据
- [x] 把 Day-0 探针升级成**通用成本探针**:输入任意路径,输出 token 口径的真实 bps 门槛 → `cost_probe.py` + [笔记](docs/week_1/通用成本探针.md)

**Week 1 交付物**:①《链上套利机会地图》笔记(含每类的成本画像);② 一个能跑任意链对/资产的 LI.FI 成本探针脚本。

### Week 2(08-12 → 08-18)· Hermes 工作流 + 从假设到策略

**目标**:把"手动看一眼"变成"系统持续盯",并提出 1–2 个你自己的套利假设去验证。

每日任务:
- [x] 用 Hermes Agent 搭研究工作流:信息发现 → 研究拆解 → 工具调用 → 数据整理 → 持续监控。把你 onChainListen 的采集能力接进来 → `lib/chainkit.py` + `watch_probe.py` + 3 个 Hermes 技能,[笔记](docs/week_2/研究工作流.md)
- 建**证据记录表**(schema 见第 4 节),给每个候选机会存档:时间、链、资产、规模、路径、成本、成败原因
- 建**完整成本模型**(见第 5 节):在 25bps 那种"纯桥费"之外,补上滑点、延迟、失败交易概率、资金占用/机会成本
- 提假设并验证:拆一个公开套利案例或你自己 Sonic 上的一笔真实清算,用历史数据 / 模拟 / paper trading 判断"还有没有 edge"

**Week 2 交付物**:①填了真实数据的证据记录表;②一个成本模型(能同时套用跨链搬砖和你的 Sonic 清算);③一份"某假设 edge 成立/不成立"的验证结论(不成立也是成果)。

### Week 3(08-19 → 08-26)· 捕获、实施、复盘 + 结业

**目标**:形成可执行的最小原型,并沉淀成你下一阶段能继续做的资产。

每日任务:
- 信号出现时的**快速三查**:真实性(价差真存在吗)、可执行性(能成交吗)、风控(最坏亏多少)
- 按自身能力选择:继续观察 / 模拟验证 / **小额严格受控**的真实执行。安全背景在这里是优势——你天然会做最坏情况分析
- 记录每次尝试的收益与失败,区分"可复现机会"和"偶然噪声"
- 收口结业三件套

**Week 3 交付物(结业)**:①**链上套利知识图谱**;②**策略候选清单**(每个附真实成本门槛和 edge 判断);③**下一阶段研究方向**(你的应该指向:跨链清算 / 长尾链清算 / 经济漏洞型 MEV)。

---

## 4. 证据记录表 schema(Week 2 开始天天填)

每发现一个候选机会,记一行。这张表就是你把"运气"变成"策略"的地方。

| 字段 | 说明 |
|---|---|
| 时间 | 发现时的区块高度 / 时间戳 |
| 链 | 源链 → 目标链(或单链) |
| 资产 | 涉及的 token |
| 类型 | DEX间 / 三角 / 稳定币 / 清算 / 跨链 / … |
| 规模 | 测试的本金规模 |
| 路径 | 用了哪个桥 / DEX / 工具(如 polymerStandard) |
| 屏幕价差 | 表面看到的价差(bps) |
| 真实成本 | token 口径的总成本(bps),含桥费+滑点+gas |
| 净收益 | 屏幕价差 − 真实成本(bps),**可能为负** |
| 延迟 | 执行需要多久(如这条路 1080 秒——价差还在吗?) |
| 成败原因 | 为什么成 / 为什么没成 |
| 可复现? | 是稳定机会还是一次性噪声 |

---

## 5. 成本模型(整个共学最值钱的东西,跨链和清算通用)

一个诚实的净收益,是**从毛价差里逐项扣掉所有摩擦**:

```
净收益 = 屏幕价差
        − 桥费 / 协议费          (你的 ARB→BAS 例子里 = 25 bps 平费)
        − 交易滑点               (深度不够时随规模上升;稳定币桥通常为 0)
        − gas 成本               (源链 + 目标链;便宜链可忽略,L1 上很咬人)
        − 延迟风险               (执行期间价差消失的概率 × 损失)
        − 失败交易成本            (失败概率 × 已花 gas + 占用的资金)
        − 资金占用 / 机会成本      (资金锁在途中的时间成本)
```

**套用到你的 Sonic 清算(这是文档给你的关键动作)**:清算最容易骗自己的地方,和跨链一模一样——"账面清算奖励"看着很香,但真正到手的要扣掉:
- gas(抢跑/被抢的竞争成本)
- **抵押物变现折价**(拿到抵押物后卖出的滑点,这就是你清算里的"滑点"项)
- 失败/被别人抢先的概率
- 资金占用

把上面这个公式改写成清算版,你就能算出"这笔清算真实到手 bps",而不是被账面奖励忽悠。**你今天在跨链上劈的这一刀,原样砍在你的主业上。**

---

## 6. 风险与红线(诚实提醒)

- **跨链搬砖是红海。** 稳定币价差扣完 25bps 基本不成立,别把时间浪费在"手动搬砖赚差价"的幻想上。你的价值在长尾/清算/经济漏洞方向。
- **别信美元标价。** 已经吃过一次幻觉了,永远 token 口径复核。
- **真实执行前先跑最坏情况。** 你有安全背景,这是天生优势——每个策略都问"我最多亏多少、怎么被抢、桥会不会出事"。
- **AI Agent(Hermes)是工具不是印钞机。** 它帮你把观察规模化,护城河很薄,别指望它自动生成 alpha。
- **这不是投资建议,共学也不承诺盈利。** 用最小规模验证,亏得起再上。

---

## 7. 工具清单

- **LI.FI API**(`https://li.quest/v1`,基础调用免 key,未认证 200 次/2 小时):`/quote` `/routes` `/chains` `/tokens` `/tools` `/connections`
- **Hermes Agent**:研究 / 信号 / 监控工作流
- **你的 onChainListen**(Python + web3.py):链上数据采集,轮询骨架已抽成 `lib/chainkit.py`,接 Hermes 的监控端
- **Foundry**:模拟执行、fork 测试(你的 bridge-lab 经验直接用)
- **Python 探针脚本**:成本核算与 edge 验证

---

## 8. 每日打卡模板(复制到你的 GitHub 仓库)

```markdown
### Day X — YYYY-MM-DD

**今天学了什么**
- (一句话主题)

**产出 / 证据**
- 代码 diff / 笔记链接 / 数据记录 / 探针输出

**新增一条铁律或反直觉发现**
- (今天有没有推翻自己昨天的某个假设?)

**证据表新增**
- (如有新候选机会,填第 4 节的表)

**明天打算**
- (一个具体动作)
```

---

## 9. 结业时我应该拥有什么

- [ ] 一张属于自己的链上套利机会地图
- [ ] 一个跨链 + 清算通用的成本模型(代码 + 公式)
- [ ] 一张填满真实数据的证据记录表
- [ ] 至少 1 个"edge 成立/不成立"的严谨结论
- [ ] 一份下一阶段研究方向:**跨链清算 / 长尾链清算 / 经济漏洞型 MEV**
- [ ] Sonic 清算主业被这套框架重新审视过一遍

> 记住共学那句话:我们不预设唯一答案,也不把学习限制在固定资料里。带着好奇心、判断力和执行力进去,用真实证据逼近答案。
```